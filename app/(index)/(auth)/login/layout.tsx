export const metadata = {
	title: 'Login | hyperbooks.',
	description: 'Log in to your account to access hyperbooks.',
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
	return <>{children}</>;
}
