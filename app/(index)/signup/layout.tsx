export const metadata = {
	title: 'Sign up | hyperbooks.',
	description: 'Sign up for an account and start invoicing.',
};

export default function SignUpLayout({ children }: { children: React.ReactNode }) {
	return <>{children}</>;
}
